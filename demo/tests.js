'use strict';

describe('being right', function() {
  it('should be the case that one and one is 2', function() {
    expect(1+1).toBe(2);
  });
});

describe('maths', function() {
  it('should be the case that one and one is 2', function() {
    expect(maths.power(2,2)).toBe(4);
  });
  it('should be the case that one and one is 2', function() {
    expect(maths.div(2,2)).toBe(4);
  });
  it('should be the case that one and one is 2', function() {
    expect(maths.div(2,2)).toBe(4);
  });
});
